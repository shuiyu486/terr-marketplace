# GetMousePointWindow

**分类:** 窗口

**签名:** `long GetMousePointWindow()`

**描述:** 获取鼠标指向的可见窗口句柄,可以获取到按键自带的插件无法获取到的句柄

## 参数

*此函数无参数。*

## 返回值

- 返回整型表示的窗口句柄

## 示例

```vbs
hwnd = dm.GetMousePointWindow()
```
