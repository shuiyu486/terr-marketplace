函数简介:

关闭屏幕保护.

函数原型:  
  
long DisableScreenSave()

参数定义:

返回值:

整形数:  
0 : 失败

1 : 成功

示例:

dm.DisableScreenSave

注 : 调用此函数后，可能在系统中还是看到屏保是开启状态。但实际上屏保已经失效了.

系统重启后，会失效。必须再重新调用一次.

此函数经常用在后台操作过程中.
避免被系统干扰.