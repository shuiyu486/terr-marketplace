函数简介:

延时指定的毫秒,过程中不阻塞UI操作. 一般高级语言使用.按键用不到.

函数原型:  
  
long Delay(mis)

参数定义:

mis整形数: 毫秒数. **必须大于0.**

返回值:  
  
整形数:  
0 : 失败

1 : 成功

示例:

dm.Delay 1000

注: 由于是com组件,调用此函数必须保证调用线程的模型为MTA.否则此函数可能会失效.