# SetPicPwd

**分类:** 图色

**签名:** `long SetPicPwd(pwd)`

**描述:** 设置图片密码，如果图片本身没有加密，那么此设置不影响不加密的图片，一样正常使用.

## 参数

| 参数 | 类型 | 说明 |
|------|------|------|
| pwd | str | 图片密码 |

## 返回值

- 0 : 失败
- 1 : 成功

## 示例

```vbs
dm_ret = dm.SetPicPwd("123")

注意,此函数必须在使用图片之前调用.
```
