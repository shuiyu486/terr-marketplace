函数简介:

获取指定文件或目录的真实路径

函数原型:  
  
string GetRealPath(path)

参数定义:

path字符串: 路径名,可以是文件路径，也可以是目录. 这里必须是全路径

返回值:  
  
字符串:  
真实路径,如果失败,返回空字符串

示例:  
  
TracePrint dm.GetRealPath("c:\11\22\3.txt")

注: 这个功能可以获取到路径中有符号链接之后的，真实路径.