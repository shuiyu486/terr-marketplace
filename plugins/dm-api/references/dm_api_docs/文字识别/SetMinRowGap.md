函数简介:

高级用户使用,在识别前,如果待识别区域有多行文字,可以设定行间距,默认的行间距是1,

如果根据情况设定,可以提高识别精度。一般不用设定。

函数原型:  
  
long SetMinRowGap(min\_row\_gap)

参数定义:  
  
min\_row\_gap 整形数:最小行间距

返回值:

整形数:  
0:失败  
1:成功

示例:

dm\_ret = dm.SetMinRowGap(2)